alias c='clear'
alias q='exit'

alias path='echo $PATH | tr ":" "\n"'
alias open='explorer.exe'

alias d='ya'

alias so='source'

alias sobash="source $BASHRC"

alias pac='pacman'
alias pacman='sudo pacman'

alias v='$EDITOR'
alias sv="sudo $EDITOR"
alias vdot="$EDITOR $DOTFILES"
alias bashrc="$EDITOR $BASHRC"
alias aliases="$EDITOR $ALIASES"

alias ls='eza -A --icons --color=auto --group-directories-first'
alias ll='ls -l'
alias l='ll --git --git-repos --no-time --no-user --no-filesize --no-permissions'
alias grep='grep --color=auto'

alias npmup='npm install -g npm@latest'

alias py='python'

alias ".."="cd .."
alias "..."="cd ../.."
alias "...."="cd ../../.."
alias "....."="cd ../../../.."
alias "......"="cd ../../../../.."
alias "......."="cd ../../../../../.."
alias "........"="cd ../../../../../../.."

alias lg='lazygit'
